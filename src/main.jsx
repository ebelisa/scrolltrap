import React from 'react'
import ReactDOM from 'react-dom/client'
import ScrollTrap from './ScrollTrap.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ScrollTrap />
  </React.StrictMode>
)
